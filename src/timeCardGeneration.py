import os
from datetime import date, timedelta
from src.databaseAccess import get_employee_name

# parse a sql query response and return a json object of time cards
def buildTimeCards(response, start, end):
    pass

    # break the response apart into lists
    # each list pertains to a single employee, determined by "id"

    # get the name of the employee that corresponds to that id
    # name = get_employee_name()





